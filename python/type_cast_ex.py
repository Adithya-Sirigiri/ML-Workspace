fir_num=input("Enter the 1st number:")
sec_num=input("Enter the 2nd number:")
sum=fir_num+sec_num
print(sum)
sum=int(fir_num)+int(sec_num)
#if we do this then we get the answer as combined because as input returns string and it is simply as we are concanating two strings so we use type casting
print(sum)